version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
    commands:
      - echo "Instalando dependencias Maven..."
      - mvn --version

  pre_build:
    commands:
      - echo "Limpiando proyecto..."
      - mvn clean

  build:
    commands:
      - echo "Ejecutando tests..."
      - mvn test
      - echo "Construyendo JAR..."
      - mvn package -DskipTests=false
      - echo "Preparando ZIP para Elastic Beanstalk..."
      - mkdir -p build-output
      - cp target/*.jar build-output/app.jar
      - echo "Creando Procfile..."
      - echo "web: java -jar app.jar" > build-output/Procfile
      - cd build-output
      - zip -r ../artifact.zip *
      - cd ..

  post_build:
    commands:
      - echo "Build completado exitosamente."

artifacts:
  files:
    - artifact.zip
  discard-paths: yes
